"""
app/api/routes/enroll.py
------------------------
PLACEHOLDER — Student / person enrollment routes.

Future implementation will:
- Accept a name and student ID via POST /enroll.
- Trigger the camera to capture N face samples.
- Extract embeddings via core/embedding.py.
- Store the embedding + identity in the database.
- Return a confirmation with the assigned identity ID.

TODO:
    - POST /enroll        — Start enrollment for a new person.
    - DELETE /enroll/{id} — Remove a person from the system.
"""
